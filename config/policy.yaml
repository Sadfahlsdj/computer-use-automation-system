allowed_origins:
  - http://127.0.0.1:8000
allowed_path_prefixes:
  - /demo
allowed_actions:
  - navigate
  - fill
  - click
  - extract
require_confirmation_for:
  - irreversible
max_steps: 30
max_retries_per_step: 2
